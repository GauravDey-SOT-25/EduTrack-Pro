import { initializeProtectedPage, bindPageRetry, setPageContent } from "../app.js";
import { getInstructorDashboardData } from "../data-service.js";
import { avatar, emptyState, errorState, progressBar, statusBadge } from "../components.js";
import { $, average, debounce, escapeHtml, formatNumber } from "../utils.js";
import { appConfig } from "../config.js";

const matchesRange = (value, filter, high, middle) => filter === "all" || (filter === "high" && value >= high) || (filter === "middle" && value >= middle && value < high) || (filter === "low" && value < middle);

const rowMarkup = (student) => `<tr class="border-b border-slate-100 last:border-0 dark:border-white/10"><td class="px-5 py-4"><div class="flex items-center gap-3">${avatar(student, "h-9 w-9")}<div class="min-w-0"><p class="truncate font-bold text-slate-800 dark:text-slate-100">${escapeHtml(student.displayName || "Unnamed student")}</p><p class="truncate text-xs text-slate-400">${escapeHtml(student.email || "")}</p></div></div></td><td class="px-5 py-4 text-sm text-slate-600 dark:text-slate-300">${escapeHtml(student.studentId || "—")}</td><td class="px-5 py-4"><p class="max-w-44 truncate text-sm text-slate-600 dark:text-slate-300">${escapeHtml(student.courses.join(", ") || "No courses")}</p></td><td class="px-5 py-4"><div class="min-w-24"><p class="mb-1 text-xs font-bold text-slate-700 dark:text-slate-200">${student.completionPercentage}%</p>${progressBar(student.completionPercentage, student.completionPercentage >= 75 ? "teal" : "amber", "Completion")}</div></td><td class="px-5 py-4 text-sm font-bold text-slate-700 dark:text-slate-200">${student.gradeAverage}%</td><td class="px-5 py-4 text-sm font-bold text-slate-700 dark:text-slate-200">${student.attendancePercentage || "—"}${student.attendancePercentage ? "%" : ""}</td><td class="px-5 py-4">${statusBadge(student.status)}</td></tr>`;

const mobileCard = (student) => `<article class="surface rounded-2xl p-4"><div class="flex items-start justify-between gap-3"><div class="flex min-w-0 items-center gap-3">${avatar(student, "h-10 w-10")}<div class="min-w-0"><p class="truncate font-bold text-slate-800 dark:text-slate-100">${escapeHtml(student.displayName || "Unnamed student")}</p><p class="truncate text-xs text-slate-400">${escapeHtml(student.email || "")}</p></div></div>${statusBadge(student.status)}</div><div class="mt-4 grid grid-cols-3 gap-3 text-center"><div><p class="text-[10px] font-bold uppercase tracking-wider text-slate-400">Progress</p><p class="mt-1 text-sm font-extrabold text-slate-800 dark:text-slate-100">${student.completionPercentage}%</p></div><div><p class="text-[10px] font-bold uppercase tracking-wider text-slate-400">Grade</p><p class="mt-1 text-sm font-extrabold text-slate-800 dark:text-slate-100">${student.gradeAverage}%</p></div><div><p class="text-[10px] font-bold uppercase tracking-wider text-slate-400">Attendance</p><p class="mt-1 text-sm font-extrabold text-slate-800 dark:text-slate-100">${student.attendancePercentage || "—"}${student.attendancePercentage ? "%" : ""}</p></div></div><p class="mt-4 truncate text-xs text-slate-500 dark:text-slate-400">${escapeHtml(student.courses.join(", ") || "No courses")}</p></article>`;

const loadStudents = async ({ profile }) => {
  try {
    const data = await getInstructorDashboardData();
    let currentPage = 1;
    setPageContent(`<section><div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h2 class="text-2xl font-extrabold tracking-tight text-slate-900 dark:text-white">Students</h2><p class="mt-2 text-sm text-slate-500 dark:text-slate-400">Search, filter, and review learner performance.</p></div><p id="student-count" class="text-sm font-bold text-slate-500 dark:text-slate-400"></p></div><div class="surface mt-6 rounded-2xl p-4"><div class="grid gap-3 md:grid-cols-2 xl:grid-cols-6"><label class="relative xl:col-span-2"><span class="sr-only">Search students</span><span class="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-slate-400">⌕</span><input id="student-search" class="w-full rounded-xl border border-slate-200 bg-white py-2.5 pl-9 pr-3 text-sm text-slate-800 outline-none transition focus:border-indigo-500 dark:border-white/10 dark:bg-slate-800 dark:text-white" placeholder="Name, email, student ID, or course"></label><select id="filter-course" class="rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm font-semibold text-slate-700 outline-none focus:border-indigo-500 dark:border-white/10 dark:bg-slate-800 dark:text-slate-200"><option value="all">All courses</option>${data.courses.map((course) => `<option value="${escapeHtml(course.title || course.id)}">${escapeHtml(course.title || course.id)}</option>`).join("")}</select><select id="filter-completion" class="rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm font-semibold text-slate-700 outline-none focus:border-indigo-500 dark:border-white/10 dark:bg-slate-800 dark:text-slate-200"><option value="all">All completion</option><option value="high">75%+ complete</option><option value="middle">50–74% complete</option><option value="low">Below 50%</option></select><select id="filter-attendance" class="rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm font-semibold text-slate-700 outline-none focus:border-indigo-500 dark:border-white/10 dark:bg-slate-800 dark:text-slate-200"><option value="all">All attendance</option><option value="high">80%+ attendance</option><option value="middle">60–79% attendance</option><option value="low">Below 60%</option></select><select id="filter-grade" class="rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm font-semibold text-slate-700 outline-none focus:border-indigo-500 dark:border-white/10 dark:bg-slate-800 dark:text-slate-200"><option value="all">All grades</option><option value="high">75%+ grade</option><option value="middle">60–74% grade</option><option value="low">Below 60%</option></select></div><div class="mt-3 flex flex-wrap items-center justify-between gap-3"><select id="filter-status" class="rounded-xl border border-slate-200 bg-white px-3 py-2 text-sm font-semibold text-slate-700 outline-none focus:border-indigo-500 dark:border-white/10 dark:bg-slate-800 dark:text-slate-200"><option value="all">All statuses</option><option value="active">Active</option><option value="at_risk">At risk</option><option value="inactive">Inactive</option></select><label class="flex items-center gap-2 text-sm font-semibold text-slate-500 dark:text-slate-400">Sort by <select id="sort-students" class="rounded-lg border border-slate-200 bg-white px-2 py-1.5 text-sm text-slate-700 outline-none focus:border-indigo-500 dark:border-white/10 dark:bg-slate-800 dark:text-slate-200"><option value="name">Name</option><option value="completion">Completion</option><option value="grade">Grade</option><option value="attendance">Attendance</option></select></label></div></div></section><section class="mt-5"><div id="student-table-wrap" class="surface hidden overflow-hidden rounded-2xl md:block"><div class="overflow-x-auto"><table class="min-w-[900px] w-full text-left"><thead class="bg-slate-50/80 text-xs uppercase tracking-wider text-slate-400 dark:bg-white/5"><tr><th class="px-5 py-3 font-bold">Student</th><th class="px-5 py-3 font-bold">ID</th><th class="px-5 py-3 font-bold">Courses</th><th class="px-5 py-3 font-bold">Completion</th><th class="px-5 py-3 font-bold">Grade</th><th class="px-5 py-3 font-bold">Attendance</th><th class="px-5 py-3 font-bold">Status</th></tr></thead><tbody id="student-results"></tbody></table></div></div><div id="student-mobile-results" class="space-y-3 md:hidden"></div><div id="student-pagination" class="mt-4 flex items-center justify-between"></div></section>`);

    const getFiltered = () => {
      const search = $("#student-search").value.trim().toLowerCase();
      const course = $("#filter-course").value;
      const completion = $("#filter-completion").value;
      const attendance = $("#filter-attendance").value;
      const grade = $("#filter-grade").value;
      const status = $("#filter-status").value;
      const sort = $("#sort-students").value;
      const rows = data.students.filter((student) => {
        const searchable = [student.displayName, student.email, student.studentId, ...student.courses].join(" ").toLowerCase();
        return (!search || searchable.includes(search)) && (course === "all" || student.courses.includes(course)) && matchesRange(student.completionPercentage, completion, 75, 50) && matchesRange(student.attendancePercentage, attendance, 80, 60) && matchesRange(student.gradeAverage, grade, 75, 60) && (status === "all" || student.status === status);
      });
      return rows.sort((left, right) => sort === "name" ? String(left.displayName || "").localeCompare(String(right.displayName || "")) : Number(right[`${sort}Percentage`] ?? right[`${sort}Average`] ?? 0) - Number(left[`${sort}Percentage`] ?? left[`${sort}Average`] ?? 0));
    };
    const renderResults = () => {
      const rows = getFiltered();
      const pages = Math.max(1, Math.ceil(rows.length / appConfig.pageSize));
      currentPage = Math.min(currentPage, pages);
      const visible = rows.slice((currentPage - 1) * appConfig.pageSize, currentPage * appConfig.pageSize);
      $("#student-count").textContent = `${formatNumber(rows.length)} learner${rows.length === 1 ? "" : "s"}`;
      $("#student-results").innerHTML = visible.length ? visible.map(rowMarkup).join("") : `<tr><td colspan="7" class="px-5 py-12 text-center text-sm text-slate-500 dark:text-slate-400">No students match these filters.</td></tr>`;
      $("#student-mobile-results").innerHTML = visible.length ? visible.map(mobileCard).join("") : emptyState({ iconName: "users", title: "No students match", description: "Try broadening your filters or search phrase." });
      $("#student-pagination").innerHTML = `<p class="text-sm text-slate-500 dark:text-slate-400">Page ${currentPage} of ${pages}</p><div class="flex gap-2"><button id="page-prev" class="rounded-lg border border-slate-200 px-3 py-1.5 text-sm font-bold text-slate-600 disabled:cursor-not-allowed disabled:opacity-40 dark:border-white/10 dark:text-slate-300" ${currentPage === 1 ? "disabled" : ""}>Previous</button><button id="page-next" class="rounded-lg border border-slate-200 px-3 py-1.5 text-sm font-bold text-slate-600 disabled:cursor-not-allowed disabled:opacity-40 dark:border-white/10 dark:text-slate-300" ${currentPage === pages ? "disabled" : ""}>Next</button></div>`;
      $("#page-prev")?.addEventListener("click", () => { currentPage -= 1; renderResults(); });
      $("#page-next")?.addEventListener("click", () => { currentPage += 1; renderResults(); });
    };
    const resetAndRender = () => { currentPage = 1; renderResults(); };
    $("#student-search").addEventListener("input", debounce(resetAndRender));
    ["#filter-course", "#filter-completion", "#filter-attendance", "#filter-grade", "#filter-status", "#sort-students"].forEach((selector) => $(selector).addEventListener("change", resetAndRender));
    renderResults();
  } catch (error) {
    setPageContent(errorState({ title: "Unable to load students", description: error.message }));
    bindPageRetry(() => loadStudents({ profile }));
  }
};

initializeProtectedPage({ allowedRoles: ["instructor"], activePage: "users", title: "Students", subtitle: "Learner directory", load: loadStudents });
